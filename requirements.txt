# MonkeyOCR v2-B Backend Requirements
# =====================================
# Install order:
#   1. pip install torch (lihat catatan CUDA di bawah)
#   2. pip install -r requirements.txt

# ── Core Vision-Language Model (MonkeyOCR v2-B backbone) ──
transformers==4.57.1
accelerate==1.11.0
qwen_vl_utils==0.0.14
# flash-attn diperlukan untuk inferensi cepat, install terpisah:
#   pip install flash-attn==2.8.3 --no-build-isolation

# ── FastAPI Server ──
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.7.0

# ── PDF Processing ──
pdfplumber==0.11.0
pdf2image==1.17.0       # butuh poppler: https://github.com/oschwartz10612/poppler-windows/releases
pillow>=10.4.0

# ── Model Download ──
huggingface-hub>=0.24.0
modelscope>=1.15.0      # untuk download dari ModelScope

# ── Utilities ──
numpy>=1.24.0
